/**
 * Higher-level resume service that wraps the low-level API client.
 * Import from here in page components and hooks.
 */
export { resumesApi } from "@/lib/api/resumes";
export type { ResumeData } from "@/lib/api/resumes";
